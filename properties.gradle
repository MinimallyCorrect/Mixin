group = "dev.minco"
description = 'Applies Mixin-style monkey patches to .java source or compiled .class files. Lightweight.'
ext.githubOwner = 'MinimallyCorrect'
ext.githubProject = 'Mixin'
ext.tags = ['mixin', 'java', 'aspect-oriented-programming', 'bytecode-manipulation']
ext.licenses = ['LGPL-3.0']

ext.githubOwnerProject = "${ext.githubOwner}/${ext.githubProject}".toString()
ext.githubUrl = "https://github.com/${ext.githubOwnerProject}".toString()
ext.website = ext.githubUrl
ext.vcsUrl = "${ext.githubUrl}.git".toString()

System.setProperty("gradle.publish.key", System.getenv("GRADLE_PUBLISH_KEY") ?: "")
System.setProperty("gradle.publish.secret", System.getenv("GRADLE_PUBLISH_SECRET") ?: "")
